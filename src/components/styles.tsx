import { Box } from "@mui/system";
import styled from "styled-components";

export const CentralContainer = styled(Box)`
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
`;

export const PageWrapper = styled(Box)`
  height: 100%;
  width: 100%;
`;
